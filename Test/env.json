{
    "paths": {
        "config": "{{path}}/config"
    },
    "nameSpaces": {
        "middleware": "Tale\\Test\\"
    },
    "middleware": [
        "{{nameSpaces.middleware}}Model",
        "{{nameSpaces.middleware}}Database",
        "{{nameSpaces.middleware}}Renderer",
        "{{nameSpaces.middleware}}Controller"
    ]
}