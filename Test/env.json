{
    "paths": {
        "config": "{{path}}/config"
    },
    "nameSpaces": {
        "plugins": "Tale\\Test\\"
    },
    "plugins": [
        "{{nameSpaces.plugins}}Model",
        "{{nameSpaces.plugins}}Database",
        "{{nameSpaces.plugins}}Renderer",
        "{{nameSpaces.plugins}}Controller"
    ]
}