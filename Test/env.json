{
    "paths": {
        "config": "{{path}}/config"
    },
    "nameSpaces": {
        "middlewares": "Tale\\Test\\"
    },
    "middlewares": [
        "{{nameSpaces.middlewares}}Model",
        "{{nameSpaces.middlewares}}Database",
        "{{nameSpaces.middlewares}}Renderer",
        "{{nameSpaces.middlewares}}Controller"
    ]
}